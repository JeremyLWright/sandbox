#include <iostream>
#include <algorithm>
#include <memory>
#include <vector>
#include <iterator>

int main(int argc, const char *argv[])
{
    std::vector<int> f(10)
    std::iota(l.begin(), l.end(), -4);
    for( auto i : f)
    {
         std::cout << i << " " << std::endl;
    }
    return 0;
}
